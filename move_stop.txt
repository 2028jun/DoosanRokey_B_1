
2. robot_controller_node.py에서 변경한 부분

2-1. MoveStop import 추가 6line
변경 전:
from dsr_msgs2.srv import GetToolForce

변경 후:
from dsr_msgs2.srv import GetToolForce, MoveStop

2-2. move_stop 서비스 클라이언트 추가
추가 위치: 87line
RobotControllerNode.__init__()에

추가 코드:
self.move_stop_client = self.node.create_client(MoveStop, '/motion/move_stop')

2-3. emergency_stop_callback에서 move_stop 먼저 호출 174 line

변경 후 코드:
self.request_motion_stop()
self.drl_script_stop(0)
self.drl_script_pause()

2-4. request_motion_stop 함수 추가 192~200 line

3. safety_manager_node.py에서 변경한 부분

3-1. MoveStop import 추가 6 line
추가 코드:
from dsr_msgs2.srv import MoveStop

3-2. move_stop 서비스 클라이언트 후보 2개 추가
추가 위치: 17~20 line
SafetyManagerNode.__init__()

추가 코드:
self.move_stop_clients = [
    ('/motion/move_stop', self.create_client(MoveStop, '/motion/move_stop')),
    ('/dsr01/motion/move_stop', self.create_client(MoveStop, '/dsr01/motion/move_stop')),
]

3-3. 긴급 정지 상태일 때 safety_manager_node가 먼저 move_stop 호출

추가 위치: 42~43 line
trigger_all_nodes()

추가 코드:
if state:
    self.request_motion_stop()


3-4. request_motion_stop 함수 추가

추가 코드: 66~75line
def request_motion_stop(self):
    for service_name, client in self.move_stop_clients:
        if client.service_is_ready():
            req = MoveStop.Request()
            req.stop_mode = 0
            client.call_async(req)
            self.get_logger().error(f"🚨 {service_name} 즉시 정지 요청을 전송했습니다.")
            return

    self.get_logger().error("❌ move_stop 서비스가 준비되지 않아 즉시 정지 요청을 보내지 못했습니다. (/motion, /dsr01/motion 모두 실패)")

